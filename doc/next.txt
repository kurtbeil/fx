1、ScalpingBollingSingal 要改成带参数的
2、关闭队列最好用c++实现一遍，
  （1）担心在不同的库中调用出现问题，
  （2）解决close队列重复的问题
5、对ScalpingBolling的可变获利区间，在图表上进行标注。
3、实现配置文件读取功能，
4、在配置文件基础上实现自动导出模块。
5、程序判断如果当前是测试状态,在deinit()函数中可以，将整个测试的结果保存到数据库，以备以后可以比对





1、学习使用对象操作函数(ok)
